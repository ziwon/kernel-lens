{
  "name": "@lkmlens/blog",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler --env-file ../../.env --config wrangler.jsonc dev --test-scheduled",
    "types": "wrangler types worker-configuration.d.ts --config wrangler.jsonc",
    "typecheck": "pnpm types && tsc --noEmit",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@lkmlens/ai": "workspace:*",
    "@lkmlens/db": "workspace:*",
    "@lkmlens/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.61.1"
  }
}
